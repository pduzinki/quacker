{{define "main"}}
<!DOCTYPE html>
<html lang="en">

    <head>
        <meta charset="utf-8">
        <title>Quacker</title>
    </head>

    <body>
        {{template "header"}}
        {{template "navbar" .}}

        <main>
            {{if .Alert}}
                {{template "alert" .Alert}}
            {{end}}

            {{template "yield" .}}
        </main>

        {{template "footer"}}
    </body>

<!DOCTYPE html>
<html lang="en">
{{end}}